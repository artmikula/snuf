import type { ScanResult, ScanOptions } from '../types.js'

export function renderMarkdown(_result: ScanResult, _options: ScanOptions): string {
  // TODO: generate full markdown report
  return '# snuf Report\n\nTODO\n'
}
